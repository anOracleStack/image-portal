# Image Portal — agent entry point

**Before any product work:** read [`docs/PROJECT-TASKS.md`](docs/PROJECT-TASKS.md).

That file is the **task list** (pending, priority order) and **work log** (append after every task).

## Required workflow

1. Open `docs/PROJECT-TASKS.md` and pick the top incomplete **Pending** item.
2. Mark progress in the **Work log** when you start and when you finish.
3. Do **not** mark tasks complete without verification (especially https://rub.pub vs local `main`).
4. Do **not** claim “all good” until audio in scope is transcribed, checklist is implemented, and production matches `main` on `/login` and other checklist routes.

## Repo facts

| Item | Value |
|------|--------|
| Production | https://rub.pub |
| Web app | `apps/web/` |
| Spec | `docs/IMAGE_PORTAL_MASTER_SPEC.md` |
| Voice notes (partial) | `docs/notes/landing-voice-memo-transcript.txt` (53 only) |
| User audio (not in repo) | `/Users/oraclevision/Downloads/Butterwood Cir {3,53,54,55,56,57}.m4a` |

## User copy rules

- Center marketing copy where requested.
- Use `&` not “and” on marketing pages.
- Use `BalancedText` for balanced line breaks where applicable.
